name: mlops-api
port: 8000

commands:
  - "gunicorn --workers 1 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 main:app"

dependencies:
  pypi:
    fastapi: ""
    gunicorn: ""
    uvicorn: ""